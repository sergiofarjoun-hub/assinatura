services:
  hamsa-agent:
    build: .
    container_name: hamsa-agent
    restart: unless-stopped
    ports:
      - "3010:3010"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - MODEL=${MODEL:-claude-opus-4-8}
    volumes:
      - ./workspace:/app/workspace
