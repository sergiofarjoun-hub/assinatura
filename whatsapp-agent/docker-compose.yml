services:
  whatsapp-agent:
    build: .
    container_name: hamsa-whatsapp-agent
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
